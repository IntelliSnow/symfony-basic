{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-12">

            {{ form_start(form) }}
            {{ form_rest(form) }}
            <button type="submit" class="btn btn-primary">Submit</button>

            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
